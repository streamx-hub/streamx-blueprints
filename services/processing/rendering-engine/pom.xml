<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.streamx.blueprints</groupId>
    <artifactId>processing</artifactId>
    <version>3.0.27</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>rendering-engine</artifactId>
  <name>StreamX Blueprints : Rendering Engine</name>

  <properties>
    <pebble.version>4.0.0</pebble.version>
    <!-- The below versions must match versions used by pebble pom.xml -->
    <caffeine.version>2.6.2</caffeine.version>
    <jakarata.servlet-api.version>5.0.0</jakarata.servlet-api.version>
    <servlet-api.version>2.5</servlet-api.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.pebbletemplates</groupId>
      <artifactId>pebble</artifactId>
      <version>${pebble.version}</version>
    </dependency>

    <!-- <optional> dependencies of pebble that are still needed while generating native image -->
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
      <version>${caffeine.version}</version>
    </dependency>
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <version>${servlet-api.version}</version>
    </dependency>
    <dependency>
      <artifactId>jakarta.servlet-api</artifactId>
      <groupId>jakarta.servlet</groupId>
      <version>${jakarata.servlet-api.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>
  </dependencies>

</project>
